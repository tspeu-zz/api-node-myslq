const config = {
    app: {
      port: 3000
    },
    db: {
      host     : 'localhost',
      user     : 'root',
      password : '',
      database : 'recetapp'
    } 
};

module.exports = config;